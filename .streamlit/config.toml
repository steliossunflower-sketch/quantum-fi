[theme]
primaryColor = "#667eea"
backgroundColor = "#ffffff"
secondaryBackgroundColor = "#f0f2f6"
textColor = "#262730"
font = "sans serif"

[server]
headless = true
# Do not set port here — cloud (Streamlit Cloud) uses 8501 for health checks

[browser]
gatherUsageStats = false
